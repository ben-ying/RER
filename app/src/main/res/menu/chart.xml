<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_sorted_by_date"
        android:orderInCategory="100"
        android:title="@string/action_sorted_by_date"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_sort_by_amount"
        android:orderInCategory="100"
        android:title="@string/action_sorted_by_amount"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_sort_by_category"
        android:orderInCategory="100"
        android:title="@string/action_sorted_by_category"
        app:showAsAction="never" />
</menu>
